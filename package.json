{
  "name": "recall-flash-card-app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test"
  },
  "dependencies": {
    "aws-amplify": "^6.20.0"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
